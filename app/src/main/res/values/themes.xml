<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Delivero" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/kPrimaryColor</item> <!-- Couleur de la barre d'action -->
        <item name="android:statusBarColor">@color/kPrimaryColor</item> <!-- Couleur de la barre de statut -->
    </style>

    <style name="Theme.Delivero" parent="Base.Theme.Delivero" />
</resources>
